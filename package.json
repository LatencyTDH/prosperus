{
  "name": "prosperus",
  "version": "0.1.0",
  "private": true,
  "description": "LLM Observability Platform",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev:server": "pnpm --filter @prosperus/server dev",
    "dev:web": "pnpm --filter @prosperus/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
